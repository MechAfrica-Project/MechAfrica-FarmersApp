name: Create Production Builds

jobs:
  build_android:
    type: build # This job type creates a production build for Android
    params:
      platform: android
  submit_android:
    type: submit # Submits the Android build to the Google Play Store
    needs:
      - build_android
    params:
      build_id: ${{ needs.build_android.outputs.build_id }}