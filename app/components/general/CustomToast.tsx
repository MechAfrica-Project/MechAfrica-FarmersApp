import CustomToastTW from './CustomToastTW';

// Provide both default and named export for compatibility with existing imports.
export default CustomToastTW;
export const CustomToast = CustomToastTW;
